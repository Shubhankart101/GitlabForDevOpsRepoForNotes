stages: [release]
release:
  image: registry.gitlab.com/gitlab-org/release-cli:latest
  script: ["echo create release"]
  release:
    tag_name: "v$CI_PIPELINE_IID"
    description: "Automated release $CI_COMMIT_SHA"
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
